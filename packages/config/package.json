{
  "name": "@cvr-bus-tracker/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared configuration for CVR Bus Tracker",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}