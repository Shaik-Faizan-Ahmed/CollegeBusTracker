/**
 * Validates bus number format (should be alphanumeric)
 */
export declare const validateBusNumber: (busNumber: string) => boolean;
/**
 * Formats timestamp for display
 */
export declare const formatTimestamp: (date: Date) => string;
/**
 * Calculates distance between two coordinates in meters
 */
export declare const calculateDistance: (lat1: number, lon1: number, lat2: number, lon2: number) => number;
/**
 * Generates unique ID
 */
export declare const generateId: () => string;
//# sourceMappingURL=index.d.ts.map